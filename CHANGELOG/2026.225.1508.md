# 2026.225.1508 — re-stamp the schema version for the remove-candy-localpkg cutover

Re-stamp this repo's `charly.yml` `version:` to 2026.225.1508, the schema
version of the cross-repo `remove-candy-localpkg` cutover (consuming spec
`4cff4d0`). The config is otherwise byte-identical — no entity composition
changes; the stamp keeps this repo's schema marker aligned with the landed
spec so the load-time gate accepts it under the post-removal schema.
Placeholder CalVer — the pr-validator finalizes it at merge.
