# 2026.234.0605 — schema re-stamp to 2026.232.0520 (spec strict-cleanup cutover)

`version:` re-stamped `2026.225.1508` → `2026.232.0520` by `charly migrate`
(the spec strict-cleanup cutover's HEAD, opencharly/charly#371). The config
carries no `install_template`, so the install-template-to-phases reshaper is
a no-op.

## R10 evidence

The charly main-repo cutover (opencharly/charly#371) ran its four class beds
with the changed binary against this re-stamped config: check-pod (steps=13,
0 warnings), check-k3s-vm, check-charly-vm, check-local-vm (steps=9 each, all
guest-side warnings on the standing allowlist). The main-repo suite loads
this box's charly.yml as a live fixture and is green.
