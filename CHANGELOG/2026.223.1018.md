# 2026.223.1018 — re-stamp the schema version for the k8s→kubernetes cutover

Re-stamp this repo's `charly.yml` `version:` to 2026.223.1018, the schema
version of the cross-repo `k8s:` → `kubernetes:` deploy-substrate-kind rename
(consuming spec `v0.2026223.1018`). The config is otherwise byte-identical —
no entity composition changes; the stamp keeps this repo's schema marker
aligned with the landed spec so the load-time gate accepts it under the
post-rename schema. Placeholder CalVer — the pr-validator finalizes it at
merge.
