# 2026.241.1006 — chore: migrate charly.yml schema to 2026.240.1943 (version bump only)

## Summary

spec #63 advanced `#SchemaVersion` from `2026.232.0520` to `2026.240.1943`. Every project repo must carry the new stamp before a consumer pinned to that spec can load it. This is a one-line version stamp produced by `charly migrate` — no structural edits.

```
-version: 2026.232.0520
+version: 2026.240.1943
```

## How tested

The previous revision of this body proved only that the old string was absent. A grep cannot show the config is *accepted* under the new schema, so the real gate is charly parsing this tree at the new spec.

`charly box validate` run against **this PR's tree**, with charly built from source at `spec v0.2026241.931` (the spec that requires schema `2026.240.1943`):

```
$ charly box validate
charly box validate EXIT=0
```

Exit 0 with no diagnostics: every box in this repo resolves and validates under the new schema. The same command against every sibling in this migration wave is also exit 0 (`distro-arch`, `distro-cachyos`, `distro-debian`, `distro-fedora`, `distro-ubuntu`), so the wave is internally consistent.

## ⚠️ Landing order

Measured on a disposable `check-pod` bed: with the **released** `plugin-deploy-pod v2026.239.1640`, a tree carrying this stamp fails at `[config]`:

```
config schema 2026.240.1943 is newer than this charly supports (max 2026.232.0520).
```

That plugin is resolved out-of-process at its own spec pin (`v0.2026232.521`, ceiling `2026.232.0520`), so this stamp puts the project above its ceiling and every pod deploy fails. **opencharly/plugin-deploy-pod#2 must merge and tag before this PR.** Full evidence is in the comment on this PR.

## Rulebook compliance

- **R1** — the bed failure this migration triggers was root-caused to the plugin's own spec pin, not worked around or waved off.
- **R2** — no parking: the blocking defect is fixed in a named, open PR (`plugin-deploy-pod#2`), and the sequencing constraint is recorded here and on all four sibling PRs.
- **R4** — no workaround; the stamp is produced by `charly migrate`, not hand-edited.
- **R6** — no force-push, no direct push to `main`, no hook bypass.
- **R7** — the gate is pasted above, run on this PR's tree.

## Change classification

- **Change class:** config (schema version stamp, 1 line)
- **Verification gate:** `charly box validate` on this PR's tree against the new schema — EXIT 0
- **Attribution tier:** `analysed on a live system` — charly parsed and validated this exact tree live at the new spec, but no code path in this repo changed and no bed passes end-to-end until `plugin-deploy-pod#2` lands, so the full gate is not yet green. Deliberately not claiming `fully tested and validated`.

*Assisted-by: Claude Code Anthropic Opus 5 (analysed on a live system)*