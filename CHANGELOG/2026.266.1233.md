# 2026.266.1233 — chore(schema): migrate the charly.yml stamp to schema HEAD 2026.261.1747

## Summary

The schema-249→261 wave leg for `distro-ubuntu` — a prerequisite for the engine-provider-class cutover (spec#144/#145). A 261 charly **rejects** a project config below its HEAD (`schema 2026.261.1747 is required (found "…")`), so the box repo's own stamp must move with the wave. `charly migrate` applied it — only the `version:` line changes (the repo authors no `cpus:`, and `disk_size:` on the kind:vm templates is retained by spec#137). Tracked in opencharly/charly#633.

## How tested

**Change-class gate (config):** `charly box validate` — the R10 matrix's config-class pre-flight — run with the 261 branch charly (`2026.266.0826`) against the migrated stamp. A 249 stamp is REJECTED by the 261 loader, so a clean validate IS the stamp's acceptance proof (the changed surface executed live):
```
$ charly box validate
charly box validate: OK — checked 108 candies, 5 boxes, 2 deploys, 7 distros, 7 builders; 0 warnings, 0 errors
```
exit status 0.

**Disposable target:** this repo's own disposable check beds (the distro bootstrap-VM beds, `disposable: true`) are the beds that compose this repo's config, exercised via opencharly/charly's R10 roster. The stamp itself is validated in place above (a version-stamp-only config — no image build, no deploy).

**Fresh-rebuild / changed-path:** the config was loaded by the 261 charly through `loaderkit`, which IS the changed acceptance surface (the 249→261 gate). No runtime code changed.

## Project-rulebook rule-compliance

- **R0** — `/charly-build:migrate`, `/charly-check:check`, `/charly-internals:git-workflow` loaded first.
- **R1** — the skew this addresses was root-caused (`GateSchemaVersion`); no environmental framing.
- **R2** — the wave is named + landed (#633).
- **R3** — one stamp per repo via the single `charly migrate`; no duplication.
- **R4** — no workaround; the sanctioned tool applied it.
- **R5** — no identifier removed; the stamp is the only change.
- **R6/R7** — `charly box validate` at the migrated stamp, 0 warnings / 0 errors.
- **R10** — the config's acceptance surface (the 261 loader gate) executed live; the repo's own disposable beds are the runtime roster.
- **Disposable-only autonomy** — no destroy performed by this change.

## Change classification

Config (`charly.yml version: stamp migration). Verification gate: `charly box validate` at the migrated stamp (0 warnings, 0 errors) with a 261 charly + the repo's disposable bed roster. Attribution tier: `analysed on a live system`.

*Assisted-by: opencode ollama-cloud/deepseek-v4.1-flash (analysed on a live system)*