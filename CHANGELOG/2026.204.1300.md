# 2026.204.1300 — Schema re-stamp to 2026.204.1223 (K5-B validation-correctness batch)

sdk's `#SchemaVersion` advanced to `2026.204.1223` (the
`strip-deploy-shell-overlay` migration-table entry, companion
`opencharly/sdk#93` merged + `opencharly/charly#182`) while this repo's
`charly.yml` `version:` stamp stayed at the prior HEAD `2026.202.0105`.
Every `LoadUnified` of this box tree would hard-error with `schema
2026.204.1223 is required (found "2026.202.0105"). Run: charly migrate`,
which would also turn `charly/.` `go test .` red on the superproject
`main` the moment the companion charly PR merged (this repo directly
loaded by `TestCandySourceDirs_OverrideAnchorsRemoteApk` /
`TestPluginInstallstepEnvelopeParity` for cachyos/fedora; the other three
join this same batch per the Cutover Sizing Law rather than trailing as
separate solo ceremonies).

Fixed via the sanctioned instrument, `charly migrate`, run standalone in
this repo. The migration's `stripDeployShellOverlay` hook is a no-op here
— this repo authors no deploy-scope `shell:` overlay (sequence-valued) on
any node, only mapping-valued candy/box `shell:` intrinsic-init blocks the
hook is designed to leave untouched — so the migration is a pure
version-stamp bump, `version: 2026.202.0105` → `version: 2026.204.1223`.

Evidence: `charly migrate` output `migrated to schema 2026.204.1223`,
idempotent on a second run (`already at schema 2026.204.1223; nothing to
migrate`); `charly box validate` exit 0; `charly box generate` one pass,
exit 0; `grep -n '^\s*shell:\s*$' charly.yml` empty (no bare sequence-valued
`shell:` key present).

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; analysed on a live system)
