# 2026.202.0156 — Schema re-stamp to 2026.202.0105 (urgent main-regression fix)

`sdk`'s `#SchemaVersion` advanced to `2026.202.0105` (the `strip-candy-libvirt-field`
migration-table entry) while this repo's `charly.yml` `version:` stamp stayed at the
prior HEAD `2026.186.2323`. Every `LoadUnified` of this box tree hard-errored with
`schema 2026.202.0105 is required (found "2026.186.2323"). Run: charly migrate`,
which also turned `charly/.` `go test .` (`TestCandySourceDirs_OverrideAnchorsRemoteApk`)
red on the superproject `main`.

Fixed via the sanctioned instrument, `charly migrate`, run standalone in this repo.
The migration's `stripCandyLibvirtField` hook is a no-op here — this repo carries no
candy-level `libvirt: [...]` field (only the unrelated `dpkg -s libvirt-daemon-system`
guest-check probes and description text mentioning libvirt) — so the migration is a
pure version-stamp bump, `version: 2026.186.2323` → `version: 2026.202.0105`.

Evidence: `charly migrate` output `migrated to schema 2026.202.0105`; `charly box
validate` exit 0 on the migrated tree.

Assisted-by: Claude Code (Anthropic Claude Sonnet 5; analysed on a live system)
